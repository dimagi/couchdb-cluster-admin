---
language: python
python: "2.7"
before_install:
  - git clone https://github.com/sstephenson/bats.git
  - cd bats
  - ./install.sh /usr/local
  - cd -
install:
  - pip install -r requirements.txt
script:
  - bats test
